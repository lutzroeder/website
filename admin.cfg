runtime=go                                      # go, node or python
server=                                         # SSH server address
user=lutzroeder                                 # SSH server user name
identity=~/.ssh/${user}_id_rsa                  # SSH server identity (public key)
site=lutzroeder                                 # Website root /var/www/${site} and systemd service name
domain=${site}.com                              # xxxx.com domain name for NGNIX reverse proxy and Let's Encryt registration
email=                                          # Email address for Let's Encrypt registration